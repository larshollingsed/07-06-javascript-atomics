var password = "sushi"
var input
while (password != input) {
  var input = prompt("Enter the password")
}

// using a "for" loop
// var input
// for (password = "sushi"; password != input; input) {
//   var input = prompt("Enter the password")
// }